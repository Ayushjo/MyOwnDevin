# Base URL of the backend API.
# - Leave empty when frontend and backend share the same origin.
# - Railway / Vercel split deploy: https://api.pullwright.iayush.com
VITE_API_URL=

# Legacy shared API token — only needed for scripts/benchmarks that bypass OAuth.
# Leave empty when using GitHub OAuth (recommended).
VITE_API_TOKEN=
